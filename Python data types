#data types #type of data type #floor-division
int
float
str
bool
list
tuple
dictionary
set
None- absence of value
#classes- custom data types- we can create our own data types
#specilized data types-they are not built into python,
#specilized data types-special modules that we can us efrom libraries.whenever we dont have data type we want can use this.extensions to the language.

#Integers
print(2+4)
print(2-4)
print(2*4)
print(2/4)

#to find type of data
print(type(2+4))-<class 'int'>
print(type(2/4))- <class 'float'>
print(type(20+1.1))-<class 'float'>

#floor division
#it is like normal division but it gives the largest possible integer.(either less than or equal to the actual answer)
print(2 // 4) #0
print(3 // 4) #0
print(10 // 2) #5
print(5 // 10) #0
print(10 // 4) #2
print(9 // 2) #4
